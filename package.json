{
  "name": "my-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "build": "echo 'No build step needed for plain Node.js' && exit 0",
    "docker-build": "docker build -t my-node-app ."
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}